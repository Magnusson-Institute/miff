Index: firefox-96.0.1/browser/components/preferences/search.js
===================================================================
--- firefox-96.0.1.orig/browser/components/preferences/search.js
+++ firefox-96.0.1/browser/components/preferences/search.js
@@ -235,6 +235,9 @@ var gSearchPane = {
     // Now clean-up and rebuild the list.
     list.removeAllItems();
     gEngineView._engineStore._engines.forEach(e => {
+      if(e.name == "Google" || e.name == "Bing" || e.name == "Amazon.com" || e.name == "eBay"){
+        gSearchPane.remove(e)
+      } else {
       let item = list.appendItem(e.name);
       item.setAttribute(
         "class",
@@ -247,7 +250,7 @@ var gSearchPane = {
       if (e.name == currentEngine) {
         list.selectedItem = item;
       }
-    });
+    }});
   },
 
   handleEvent(aEvent) {
Index: firefox-96.0.1/toolkit/components/search/SearchService.jsm
===================================================================
--- firefox-96.0.1.orig/toolkit/components/search/SearchService.jsm
+++ firefox-96.0.1/toolkit/components/search/SearchService.jsm
@@ -503,7 +503,7 @@ SearchService.prototype = {
    *   The engine that is default.
    */
   _originalDefaultEngine(privateMode = false) {
-    let defaultEngine = this._getEngineByWebExtensionDetails(
+/*    let defaultEngine = this._getEngineByWebExtensionDetails(
       privateMode && this._searchPrivateDefault
         ? this._searchPrivateDefault
         : this._searchDefault
@@ -521,7 +521,8 @@ SearchService.prototype = {
 
     // Something unexpected as happened. In order to recover the original
     // default engine, use the first visible engine which is the best we can do.
-    return this._sortedVisibleEngines[0];
+    return this._sortedVisibleEngines[0];*/
+    return this._sortedVisibleEngines.filter(e=>e._name == "DuckDuckGo")[0];
   },
 
   /**
